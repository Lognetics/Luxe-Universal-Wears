# Luxe Universal Wears — environment variables
# Copy to .env.local and fill in once the Supabase project is created.
# The site runs fully WITHOUT these (catalog from local data, auth in demo mode).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
